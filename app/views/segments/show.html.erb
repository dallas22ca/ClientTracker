<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @segment.name %> (<%= @segment.segmentizations_count %> contacts)
</p>

<p>
  <strong>Conditions:</strong>
  <%= @segment.conditions %>
</p>

<ul>
	<% @segment.contacts.each do |contact| %>
		<li><%= link_to contact.key, contact %></li>
	<% end %>
</ul>

<%= link_to 'Edit', edit_segment_path(@segment) %> |
<%= link_to 'Back', segments_path %>
